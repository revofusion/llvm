Implement /Users/revofusion/Projects/llvm/plans/pass-cir-48-lit-failures.md end-to-end in /Users/revofusion/Projects/llvm on branch main. Deliverable: make `build/bin/llvm-lit -s clang/test/CIR/CodeGen clang/test/CIR/IR` pass 258/258 while preserving soundness. Constraints: do production fixes before FileCheck updates; never bless FileCheck drift without semantic comparison to test intent and OGCG where applicable; do not push; leave changes ready for review. Existing plan phases: member-pointer zero-initialization soundness; bitfield constant memory retargeting; forward-decls recursive output decision/fix; per-file FileCheck refresh with semantic review; final verification ladder.

Success criteria:
1. member_pointer_null_soundness: The CIR path must not emit `#cir.zero` for an Itanium data member pointer null in `clang/test/CIR/CodeGen/nonzeroinit-struct.cpp`. Automated test: `build/bin/llvm-lit -sv clang/test/CIR/CodeGen/nonzeroinit-struct.cpp`, with RED evidence before implementation and GREEN after. Manual QA channel: tmux session `ulw-qa-member-pointer` running `build/bin/clang -cc1 -internal-isystem build/lib/clang/22/include -nostdsysteminc -triple x86_64-unknown-linux-gnu -fclangir -emit-cir clang/test/CIR/CodeGen/nonzeroinit-struct.cpp -o -` and checking the transcript contains a correct null data member pointer representation or a precise NYI, never `cir.global external @t = #cir.zero : !rec_Trivial`. Artifact: `.omo/ulw-loop/evidence/member-pointer.transcript`.
2. bitfield_constant_retargeting: `constant-inits.cpp` and `struct-init.cpp` must compile and check, with byte-exact initialized memory preserved. Automated test: `build/bin/llvm-lit -sv clang/test/CIR/CodeGen/constant-inits.cpp clang/test/CIR/CodeGen/struct-init.cpp`, RED before implementation and GREEN after. Manual QA channel: tmux session `ulw-qa-bitfield-retarget` running the same lit command plus direct `-emit-cir` snippets, artifact `.omo/ulw-loop/evidence/bitfield-retarget.transcript`.
3. forward_decls_recursive_output: `forward-decls.cpp` must pass after deciding whether incomplete recursive records are canonical or producer completion needs fixing. Automated test: `build/bin/llvm-lit -sv clang/test/CIR/CodeGen/forward-decls.cpp`, RED before and GREEN after. Manual QA channel: tmux session `ulw-qa-forward-decls` running the lit command and showing CHECK5 passes, artifact `.omo/ulw-loop/evidence/forward-decls.transcript`.
4. full_codegen_ir_gate: The full 48-failure gate must pass with no regressions in the requested surface. Automated test: `build/bin/llvm-lit -s clang/test/CIR/CodeGen clang/test/CIR/IR`, RED baseline and GREEN final. Manual QA channel: tmux session `ulw-qa-full-cir-gate` running `ninja -C build clang split-file FileCheck && build/bin/llvm-lit -s clang/test/CIR/CodeGen clang/test/CIR/IR`, artifact `.omo/ulw-loop/evidence/full-cir-gate.transcript`.
5. adjacent_regression_broad: The broader CIR/broad sample surfaces must be measured and regressions reported. Automated tests: `build/bin/llvm-lit -s clang/test/CIR` and `python3 /tmp/broad.py base/` if present. Manual QA channel: tmux session `ulw-qa-adjacent-regression` running those commands, artifact `.omo/ulw-loop/evidence/adjacent-regression.transcript`.
